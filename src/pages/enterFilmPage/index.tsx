import React from 'react';
import { Layout } from '../layout';
import { EnterFilm } from '../../features/enterFilm';

export const EnterFilmPage = () => {
	return (
		<Layout>
			<EnterFilm />
		</Layout>
	);
};
